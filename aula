#include <stdio.h>

int main() {
    float altura= 1.75; // altura em metros
    float peso = 70.5; // peso em kg
    float imc = peso / (altura * altura); // cáculo do imc
    printf("O IMC é: %.2f\n", imc); // exibir o resultado com casas decimais 

    return 0;
}