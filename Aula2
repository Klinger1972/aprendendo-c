#include <stdio.h>

int main() {
    char nome[50];

    printf("Digite seu nome: ");
    fgets(nome, sizeof(nome), stdin);

    puts("Olá,");
    fprintf(stdout, "Bem-vindo, %s\n", nome);

    return 0;
}